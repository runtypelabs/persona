---
"@runtypelabs/persona": minor
---

Add `iconColor` to rich suggestion items. Any CSS color tints that item's glyph alone, leaving the label, border, and background neutral, so a set can be color-coded without promoting one item through `emphasis: "primary"`.
