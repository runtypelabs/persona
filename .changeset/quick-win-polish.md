---
"@runtypelabs/persona": patch
---

Context mention polish: repeated screen-reader announcements now re-speak (live region clears and sets across separate tasks), the documented `item.group` menu header override is now honored, the mention content segment embeds the full mention ref instead of copying its fields, and the unused composer document APIs (`blocksFromMessage`, `toPlainText`, `ComposerMentionState`, `setDocument`) were removed before first release.
