---
"@runtypelabs/persona": patch
---

Export the `AgentWidgetStorageAdapter` and `AgentWidgetStoredState` types from the package root, and the `CompareMode` and `PreviewShellMode` types from `@runtypelabs/persona/theme-editor`.
