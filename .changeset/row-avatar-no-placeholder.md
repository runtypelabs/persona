---
"@runtypelabs/persona": patch
---

Drop the placeholder row avatar in the Messages list: rows are text-only unless the host provides a real mark (features.history.rowAvatar, or the launcher's iconUrl). The merged launcher default no longer puts the generic chat glyph on every row.
