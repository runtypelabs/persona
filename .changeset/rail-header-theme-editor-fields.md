---
"@runtypelabs/persona": patch
---

Expose the header band tokens in the theme editor. Header Controls gains a header min height field, and two new sections cover the Messages rail header: Messages Rail Header (min height, bottom border) and Messages Rail Header Colors (background). THEME-CONFIG documents the rail as one topic: the rail header tokens, welding the two strips to one height, the rail's `--persona-history-*` behavior including the dark-theme wash caveat, and the `features.history.rail` geometry and collapse keys.
