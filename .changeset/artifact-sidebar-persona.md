---
"@runtypelabs/persona": minor
---

Add optional artifact sidebar: SSE handling for artifact events, in-session artifact store, split-pane / mobile drawer UI, `features.artifacts`, and controller / `persona:*` window hooks. Dispatch payloads accept optional `artifacts` on agent config (API parity). Includes demo page in `examples/embedded-app/artifact-demo.html`.
