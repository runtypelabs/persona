---
"@runtypelabs/persona": minor
---

Add runtime update support for `statusIndicator.align`, `layout.contentMaxWidth`, and `copy.showWelcomeCard` so these properties update the widget preview dynamically without requiring a rebuild.

Expand theme reference documentation with `approval`, `textToSpeech`, `messageActions`, `attachments`, `markdown`, and expanded `toolCall` and `layout` entries.
