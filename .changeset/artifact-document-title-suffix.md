---
"@runtypelabs/persona": patch
---

Document artifact toolbar title: strip a trailing `· MD` from `artifact` titles before appending ` · MD` so streams that already include the suffix are not shown twice.
