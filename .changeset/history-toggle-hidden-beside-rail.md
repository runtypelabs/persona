---
"@runtypelabs/persona": patch
---

Hide the shell header's Messages toggle whenever a history rail surface is present, so a docked rail, an icon rail or an overlay trigger is the only conversation-list control on screen. The header toggle returns once Messages is fully closed, and panel presentation is unchanged.
