---
"@runtypelabs/persona": patch
---

Match the Messages list skeleton to the rendered row anatomy. Each skeleton row now mirrors a real row: a head line with a title bar on the left and a timestamp stub on the right, a preview bar under it, centered in the same row box with no extra gap between rows, so loaded rows land where the skeleton stood. The rail presentation keeps title-only skeleton rows.
