---
"@runtypelabs/persona": minor
---

Artifact tab strip: accessible scrollable default with edge fade, plus a `renderTabBar` hook to fully replace the tab bar and an exported `createRovingTablist` helper for accessible custom bars.
