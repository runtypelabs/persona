---
"@runtypelabs/persona": patch
---

Inline embed (`launcher.enabled: false`) with `launcher.fullHeight: true` now sizes the panel to 100% of the host mount width instead of the default launcher width (`min(400px, …)`).
