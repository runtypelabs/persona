---
"@runtypelabs/persona": minor
---

Make the approval bubble's box-shadow themeable, matching how its background/border already work. Set it globally via the `components.approval.requested.shadow` theme token (or the `--persona-approval-shadow` CSS variable), or per-widget via `config.approval.shadow` (pass `"none"` to remove it).
