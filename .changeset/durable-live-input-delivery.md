---
"@runtypelabs/persona": minor
---

Add opt-in live input joining for native durable client-token chat. Send messages without cancelling the running response or pending tools, track each delivery, safely retry uncertain sends, and stop the durable run explicitly. Gate initial history reconciliation before sending so the first message cannot disappear during startup.
