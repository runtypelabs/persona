---
"@runtypelabs/persona": minor
---

Add a rendered/source view toggle to the inline artifact chrome, using the same segmented toggle group the artifact pane uses so the two surfaces match. On complete file-backed markdown artifacts that have a rendered alternative to their source (previewable HTML/SVG, or markdown-kind files), the inline block now shows a per-block segmented control with "Rendered view" and "Source" segments that flips the body between the preview and the raw highlighted source; the group's sliding-thumb animation is the toggle's motion cue (the body swaps instantly, matching the pane). Default on; availability-gated (hidden while streaming, for plain markdown / component / source-only "other" files, when `filePreview.enabled: false`, and when `inlineBody.viewMode: "source"` forces a source-only body). Opt out with `features.artifacts.inlineChrome: { showViewToggle: false }`. The segmented pill also now sizes its segments so its outer height matches the neighboring toolbar icon buttons, in both the pane and inline chrome.
