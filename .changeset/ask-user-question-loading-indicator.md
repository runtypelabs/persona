---
"@runtypelabs/persona": patch
---

Show the typing indicator immediately after answering an `ask_user_question` (and after approving/denying a tool), so the gap between the user's click and the next streamed token no longer feels broken.
