/** First-hover wait. Matches ChatGPT and Claude. */
export const DEFAULT_TOOLTIP_DELAY_MS = 200;
/** After one tooltip, later ones in this window open immediately. */
export const DEFAULT_TOOLTIP_SKIP_DELAY_MS = 300;
